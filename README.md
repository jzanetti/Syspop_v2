# Syspop

This package is used to create synthetic population dataset from aggregated, open source dataset.


This package generates a synthetic unit record dataset from the 2023 New Zealand Census aggregated data.

The package can be installed as:
```
mamba env create -f env.yml
```